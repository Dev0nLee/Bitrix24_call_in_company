<!DOCTYPE html>
<html>
<head>
    <title>Редактирование данных клиента</title>
    <style>
        .call-card-form {
            font-family: Arial, sans-serif;
            padding: 10px;
        }
        .call-card-form label {
            display: block;
            margin: 5px 0;
        }
        .call-card-form input {
            width: 100%;
            padding: 5px;
            margin-bottom: 10px;
        }
        .call-card-form button {
            background-color: #28a745;
            color: white;
            padding: 8px 16px;
            border: none;
            cursor: pointer;
        }
        .call-card-form button:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <div class="call-card-form">
        <h3>Редактирование данных клиента</h3>
        {% if entityType is defined and entityId is defined %}

            <form id="call-card-form" method="POST" action="{{ path('call_card', {'call_id': callId}) }}">
                <label for="name">Имя/Название:</label>
                <input type="text" id="name" name="name" value="{{ entityData.NAME ?? entityData.TITLE ?? '' }}" placeholder="Имя или название">

                <label for="phone">Телефон:</label>
                <input type="text" id="phone" name="phone" value="{{ entityData.PHONE[0].VALUE ?? '' }}" placeholder="Телефон">

                <label for="email">Email:</label>
                <input type="text" id="email" name="email" value="{{ entityData.EMAIL[0].VALUE ?? '' }}" placeholder="Email">

                <button type="submit">Сохранить</button>
            </form>
        {% else %}
            <p>Загрузка данных...</p>
        {% endif %}
    </div>
</body>
</html>